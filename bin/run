#!/bin/bash

if [[ $(head -n1 $1 | grep '^#!') ]]; then
  echo $(head -n1 $1 | grep '^#!' | sed 's/^#!//') $1 | sh
elif [[ -e $(pwd)/.runrc ]]; then
  . $(pwd)/.runrc
elif [[ -e $HOME/.runrc ]]; then
  . $HOME/.runrc
fi

$@
